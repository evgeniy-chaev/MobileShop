<Window x:Class="MobileShop.MobilePhoneUpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MobileShop"
        mc:Ignorable="d"
        Title="" Height="400" Width="360">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Название:" />

        <TextBox Text="{Binding ProductName}" Grid.Column="1"/>

        <TextBlock Text="Производитель:" Grid.Row="1"/>

        <TextBox Text="{Binding Manufacturer}" Grid.Row="1" Grid.Column="1"/>

        <TextBlock Text="Дисплей:" Grid.Row="2"/>

        <TextBox Text="{Binding Display}" Grid.Row="2" Grid.Column="1"/>

        <TextBlock Text="Связь:" Grid.Row="3"/>

        <TextBox Text="{Binding Network}" Grid.Row="3" Grid.Column="1"/>

        <TextBlock Text="Процессор:" Grid.Row="4"/>

        <TextBox Text="{Binding CPU}" Grid.Row="4" Grid.Column="1"/>

        <TextBlock Text="Память (ОЗУ/ПЗУ):" Grid.Row="5"/>

        <TextBox Text="{Binding Memory}" Grid.Row="5" Grid.Column="1"/>

        <TextBlock Text="Камера:" Grid.Row="6"/>

        <TextBox Text="{Binding Camera}" Grid.Row="6" Grid.Column="1"/>

        <TextBlock Text="Аккумулятор:" Grid.Row="7"/>

        <TextBox Text="{Binding Battery}" Grid.Row="7" Grid.Column="1"/>

        <TextBlock Text="Остаток:" Grid.Row="8"/>

        <TextBox Text="{Binding Count}" Grid.Row="8" Grid.Column="1"/>

        <TextBlock Text="Цена:" Grid.Row="9"/>

        <TextBox Text="{Binding Price}" Grid.Row="9" Grid.Column="1"/>

        <TextBlock Text="Изображение:" Grid.Row="10"/>

        <Grid Grid.Row="10" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding ImagePath}"/>

            <Button Click="OpenFileDialog_Click" Content="..." Grid.Column="1" Width="20" Margin="0 5 5 5"/>
        </Grid>

        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Orientation="Horizontal"
                    Grid.Row="11" Grid.Column="1">

            <Button IsDefault="True" Click="Accept_Click" Content="OK" MinWidth="60"/>

            <Button IsCancel="True" Content="Отмена" MinWidth="60"/>
        </StackPanel>
    </Grid>
</Window>
